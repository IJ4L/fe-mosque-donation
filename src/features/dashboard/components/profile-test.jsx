import React from "react";

// Define a simple component with explicit export
const Profile = () => {
  return (
    <div>
      <h1>Simple Profile Component</h1>
      <p>This is a minimal version to test the default export.</p>
    </div>
  );
};

// Explicitly export the component as default
export default Profile;
